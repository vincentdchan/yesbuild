{
  "name": "yesbuild",
  "version": "1.0.0",
  "main": "dist/index.js",
  "author": "Vincent Chan <okcdz@diverse.space>",
  "license": "MIT",
  "scripts": {
    "build": "esbuild --bundle src/index.ts --outdir=dist --platform=node --minify --external:esbuild --sourcemap",
    "watch": "esbuild --bundle src/index.ts --outdir=dist --platform=node --minify --external:esbuild --sourcemap --watch"
  },
  "dependencies": {
    "esbuild": "^0.13.15"
  },
  "devDependencies": {
    "@types/node": "^16.11.10",
    "cac": "^6.7.12",
    "js-yaml": "^4.1.0"
  }
}
